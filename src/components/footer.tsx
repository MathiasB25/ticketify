import { component$ } from '@builder.io/qwik';

export default component$(() => {

    return (
        <footer>
            <div class="border-t text-center py-2">
                <span class="text-sm text-neutral-600">Ticketify by MathiasB25</span>
            </div>
        </footer>
    );
});